public class Subtraction
{
    public static void Subtract(int operation1, int operation2){
        int Result= operation1 - operation2;
        System.out.println(operation1 + "-" + operation2 + "=" + Result );

        
    }

    /*create a function that takes in two integers.
        It subtracts the second from the first and prints the equation with the result.
        ie: "2 - 3 = -1"
        Afterwards, create a case in the switch statement in Main.java and call your function.
    */
}
