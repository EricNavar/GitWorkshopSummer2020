class Exponent {
    public static void exponent(int operator1, int operator2) {
        double result = Math.pow(operator1, operator2);
        System.out.println(operator1 + " ^ " + operator2 + " = " + result);
    }
}
//3 2
//3 ^ 2 =9